suite: Test Helper Name Override Coverage
templates:
  - hyperdx/service.yaml
values:
  - values/name-override.yaml
tests:
  - it: should use nameOverride in service selector labels
    asserts:
      - equal:
          path: spec.selector["app.kubernetes.io/name"]
          value: custom-stack
