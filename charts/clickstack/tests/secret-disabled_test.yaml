suite: Test ClickStack Secret Disabled
templates:
  - hyperdx/secret.yaml
values:
  - values/secret-disabled.yaml
tests:
  - it: should not render secret when secrets is null and subcharts disabled
    asserts:
      - hasDocuments:
          count: 0
