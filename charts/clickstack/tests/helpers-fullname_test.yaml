suite: Test Helper Fullname Override Coverage
templates:
  - hyperdx/deployment.yaml
values:
  - values/fullname-override.yaml
tests:
  - it: should use fullnameOverride for deployment name
    asserts:
      - equal:
          path: metadata.name
          value: hyperdx-api
